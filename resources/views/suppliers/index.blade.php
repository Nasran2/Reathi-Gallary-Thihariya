@extends('layouts.app')
@section('title','Suppliers')
@section('content')
<div x-data="{add:false}" class="space-y-5"><div class="flex flex-wrap justify-between gap-3"><div><h1 class="font-serif text-3xl text-ink">Suppliers</h1><p class="text-sm text-slate-500">Purchases, pending payments and supplier payables.</p></div><button class="btn-teal" @click="add=true">+ Add Supplier</button></div>
<form class="card grid gap-3 p-4 md:grid-cols-4"><input class="md:col-span-2" name="q" value="{{ request('q') }}" placeholder="Name, company or phone"><select name="status"><option value="all">All statuses</option><option value="active" @selected(request('status')==='active')>Active</option><option value="inactive" @selected(request('status')==='inactive')>Inactive</option></select><button class="btn-primary">Search</button></form>
<div class="card overflow-x-auto"><table><thead><tr><th>Supplier</th><th>Phone</th><th>Total Bills</th><th>Total Purchases</th><th>Pending</th><th>Current Due</th><th>Last Purchase</th><th>Status</th><th>Actions</th></tr></thead><tbody>@forelse($records as $s)<tr><td><a class="font-semibold text-teal" href="{{ route('suppliers.show',$s) }}">{{ $s->name }}</a><div class="text-xs text-slate-400">{{ $s->company }}</div></td><td>{{ $s->phone ?: '—' }}</td><td>{{ $s->purchases_count }}</td><td>Rs. {{ number_format($s->purchases_total ?? 0,2) }}</td><td>Rs. {{ number_format($s->pending_balance,2) }}</td><td class="font-bold text-amber-700">Rs. {{ number_format(max(0,(float)$s->balance-(float)$s->pending_balance),2) }}</td><td>{{ $s->last_purchase_at?\Carbon\Carbon::parse($s->last_purchase_at)->format('d M Y'):'Never' }}</td><td><span class="badge {{ $s->active?'bg-emerald-50 text-emerald-700':'bg-slate-100' }}">{{ $s->active?'Active':'Inactive' }}</span></td><td><div class="flex gap-1"><a class="btn-soft !px-2 !py-1" href="{{ route('suppliers.show',$s) }}">View</a>@if((float)$s->balance>(float)$s->pending_balance)<a class="btn !bg-amber-100 !px-2 !py-1 text-amber-800" href="{{ route('suppliers.show',[$s,'pay'=>1]) }}">Pay</a>@endif<form method="post" action="{{ route('suppliers.destroy',$s) }}" onsubmit="return confirm('Delete unused supplier or deactivate if transactions exist?')">@csrf @method('DELETE')<button class="btn !bg-red-50 !px-2 !py-1 text-red-700">{{ $s->purchases_count?'Deactivate':'Delete' }}</button></form></div></td></tr>@empty<tr><td colspan="9" class="py-10 text-center text-slate-400">No suppliers.</td></tr>@endforelse</tbody></table></div>{{ $records->links() }}
<div x-show="add" x-cloak class="fixed inset-0 z-50 grid place-items-center bg-slate-950/50 p-4"><form method="post" action="{{ route('suppliers.store') }}" @click.outside="add=false" class="card w-full max-w-3xl p-6">@csrf<div class="mb-4 flex justify-between"><h2 class="font-serif text-2xl">Add Supplier</h2><button type="button" @click="add=false">✕</button></div><div class="grid gap-4 md:grid-cols-3"><div><label>Name *</label><input class="w-full" name="name" required></div><div><label>Company</label><input class="w-full" name="company"></div><div><label>Phone</label><input class="w-full" name="phone"></div><div><label>WhatsApp</label><input class="w-full" name="whatsapp"></div><div><label>Email</label><input class="w-full" name="email" type="email"></div><div><label>Tax Number</label><input class="w-full" name="tax_number"></div><div><label>Opening Balance</label><input class="w-full" name="opening_balance" type="number" step=".01" value="0"></div><div><label>Address</label><input class="w-full" name="address"></div><div><label>Notes</label><input class="w-full" name="notes"></div></div><div class="mt-5 flex justify-end"><button class="btn-teal">Save Supplier</button></div></form></div></div>
@endsection
