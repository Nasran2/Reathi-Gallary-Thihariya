@extends('layouts.app') @section('title','Sales') @section('content')<div class="mb-6 flex justify-between"><div><h1 class="font-serif text-3xl text-ink">Sales</h1><p class="text-sm text-slate-500">Completed main and remnant invoices with frozen historical profit.</p></div><div class="flex gap-2"><a class="btn-soft" href="{{ route('pos.remnant') }}">Remnant sale</a><a class="btn-teal" href="{{ route('pos.main') }}">Main sale</a></div></div><div class="card overflow-x-auto"><table><thead><tr><th>Invoice</th><th>Date</th><th>Customer</th><th>Type</th><th>Total</th><th>Paid</th><th>Due</th><th>Profit / loss</th></tr></thead><tbody>@forelse($sales as $s)<tr><td><a class="font-semibold text-teal" href="{{ route('sales.show',$s) }}">{{ $s->invoice_no }}</a></td><td>{{ $s->sold_at->format('d M Y H:i') }}</td><td>{{ $s->customer?->name??'Walk-in' }}</td><td><span class="badge {{ $s->sale_type==='remnant'?'bg-amber-50 text-amber-700':'bg-teal-50 text-teal-700' }}">{{ ucfirst($s->sale_type) }}</span></td><td>Rs. {{ number_format($s->grand_total,2) }}</td><td>Rs. {{ number_format($s->paid_total,2) }}</td><td>Rs. {{ number_format($s->due_total,2) }}</td><td class="font-semibold {{ $s->profit_total<0?'text-red-600':'text-emerald-600' }}">Rs. {{ number_format($s->profit_total,2) }}</td></tr>@empty<tr><td colspan="8" class="py-12 text-center text-slate-400">No completed sales yet.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $sales->links() }}</div>@endsection
