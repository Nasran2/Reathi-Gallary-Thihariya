@extends('layouts.app') @section('title','Purchases') @section('content')<div class="mb-6 flex items-center justify-between"><div><h1 class="font-serif text-3xl text-ink">Purchases</h1><p class="text-sm text-slate-500">Only received purchases change stock.</p></div>@can('manage-purchases')<a class="btn-teal" href="{{ route('purchases.create') }}">+ Receive purchase</a>@endcan</div><div class="card overflow-x-auto"><table><thead><tr><th>Purchase</th><th>Date</th><th>Supplier</th><th>Supplier invoice</th><th>Invoice cost</th><th>Extra cost</th><th>Status</th></tr></thead><tbody>@forelse($purchases as $p)<tr><td><a class="font-semibold text-teal" href="{{ route('purchases.show',$p) }}">{{ $p->purchase_no }}</a></td><td>{{ $p->purchase_date->format('d M Y') }}</td><td>{{ $p->supplier->name }}</td><td>{{ $p->supplier_invoice_no??'—' }}</td><td>Rs. {{ number_format($p->supplier_total,2) }}</td><td>Rs. {{ number_format($p->extra_cost_total,2) }}</td><td><span class="badge bg-emerald-50 text-emerald-700">{{ ucfirst($p->status) }}</span></td></tr>@empty<tr><td colspan="7" class="py-12 text-center text-slate-400">No purchases recorded.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $purchases->links() }}</div>@endsection
