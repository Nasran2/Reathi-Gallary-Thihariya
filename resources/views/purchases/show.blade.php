@extends('layouts.app') @section('title',$purchase->purchase_no) @section('content')<div class="mb-6 flex justify-between"><div><a class="text-sm text-slate-500" href="{{ route('purchases.index') }}">← Purchases</a><h1 class="mt-2 font-serif text-3xl text-ink">{{ $purchase->purchase_no }}</h1><p class="text-sm text-slate-500">{{ $purchase->supplier->name }} · {{ $purchase->purchase_date->format('d M Y') }}</p></div><span class="badge h-fit bg-emerald-50 text-emerald-700">Received</span></div><div class="card overflow-x-auto"><table><thead><tr><th>Product</th><th>Purchase quantity</th><th>Base quantity</th><th>Supplier cost</th><th>Invoice total</th><th>Allocated extra</th><th>Landed/base</th><th>New avg</th></tr></thead><tbody>@foreach($purchase->items as $i)<tr><td><div class="font-semibold">{{ $i->product->name }}</div><div class="text-xs text-slate-400">{{ $i->product->sku }}</div></td><td>{{ $i->quantity+0 }} {{ $i->unit->symbol }}</td><td>{{ $i->base_quantity+0 }} {{ $i->product->baseUnit?->symbol }}</td><td>Rs. {{ number_format($i->supplier_unit_cost,4) }}</td><td>Rs. {{ number_format($i->supplier_line_total,2) }}</td><td>Rs. {{ number_format($i->allocated_extra_cost,2) }}</td><td>Rs. {{ number_format($i->landed_unit_cost,6) }}</td><td>Rs. {{ number_format($i->new_average_cost,6) }}</td></tr>@endforeach</tbody></table></div><div class="mt-5 ml-auto grid max-w-lg grid-cols-2 gap-3"><div class="card p-4"><div class="text-xs text-slate-400">Supplier payable</div><div class="mt-1 text-xl font-bold">Rs. {{ number_format($purchase->supplier_total,2) }}</div></div><div class="card p-4"><div class="text-xs text-slate-400">Landed inventory value</div><div class="mt-1 text-xl font-bold">Rs. {{ number_format($purchase->supplier_total+$purchase->extra_cost_total,2) }}</div></div></div>@endsection
