@extends('layouts.app') @section('title','Stock Ledger') @section('content')<div class="mb-6 flex justify-between"><div><h1 class="font-serif text-3xl text-ink">Stock movement ledger</h1><p class="text-sm text-slate-500">Every quantity change, in base stock units.</p></div><a class="btn-soft" href="{{ route('inventory.index') }}">Inventory</a></div><div class="card overflow-x-auto"><table><thead><tr><th>Date</th><th>Product</th><th>Inventory</th><th>Movement</th><th>Qty in</th><th>Qty out</th><th>Balance</th><th>Cost</th><th>User</th></tr></thead><tbody>@forelse($movements as $m)<tr><td>{{ $m->created_at->format('d M Y H:i') }}</td><td><div class="font-semibold">{{ $m->product->name }}</div><div class="text-xs text-slate-400">{{ $m->notes }}</div></td><td><span class="badge {{ $m->inventory_type==='remnant'?'bg-amber-50 text-amber-700':'bg-teal-50 text-teal-700' }}">{{ ucfirst($m->inventory_type) }}</span></td><td>{{ str($m->movement_type)->replace('_',' ')->title() }}</td><td class="text-emerald-600">{{ $m->quantity_in>0?'+'.number_format($m->quantity_in,4):'—' }}</td><td class="text-red-600">{{ $m->quantity_out>0?'-'.number_format($m->quantity_out,4):'—' }}</td><td>{{ number_format($m->balance_after,4) }} {{ $m->product->baseUnit->symbol }}</td><td>@can('view-cost')Rs. {{ number_format($m->unit_cost,4) }}@else — @endcan</td><td>{{ $m->user?->name??'System' }}</td></tr>@empty<tr><td colspan="9" class="py-12 text-center text-slate-400">No movements posted.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $movements->links() }}</div>@endsection
