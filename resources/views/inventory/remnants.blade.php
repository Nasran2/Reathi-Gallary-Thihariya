@extends('layouts.app') @section('title','Remnants') @section('content')<div class="mb-6 flex justify-between"><div><h1 class="font-serif text-3xl text-ink">Remnant & cut-piece lots</h1><p class="text-sm text-slate-500">Individually traceable irregular pieces.</p></div>@can('transfer-remnant')<a class="btn bg-amber-500 text-white" href="{{ route('remnants.transfer') }}">+ Transfer from main</a>@endcan</div><div class="card overflow-x-auto"><table><thead><tr><th>Remnant</th><th>Product</th><th>Original</th><th>Remaining</th><th>Normal price</th><th>Remnant price</th><th>Cost / base</th><th>Status</th></tr></thead><tbody>@forelse($remnants as $r)<tr><td><div class="font-semibold text-amber-700">{{ $r->remnant_no }}</div><div class="text-xs text-slate-400">{{ $r->barcode }}</div></td><td>{{ $r->product->name }}</td><td>{{ $r->original_quantity+0 }} {{ $r->unit->symbol }}</td><td class="font-semibold">{{ $r->remaining_quantity+0 }} {{ $r->unit->symbol }}</td><td>Rs. {{ number_format($r->normal_price,2) }}</td><td>Rs. {{ number_format($r->remnant_price,2) }}</td><td>@can('view-cost')Rs. {{ number_format($r->cost_per_base_unit,4) }}@else—@endcan</td><td><span class="badge bg-amber-50 text-amber-700">{{ str($r->status)->replace('_',' ')->title() }}</span></td></tr>@empty<tr><td colspan="8" class="py-12 text-center text-slate-400">No remnants created.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $remnants->links() }}</div>@endsection
